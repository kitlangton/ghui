---
"@kitlangton/ghui": minor
---

Add a docked Changed Files panel to the diff view. Auto-shows on terminals ≥ 130 cols, with `shift+f` to toggle visibility. The existing `f` "open changed files" key now renders inline in the panel when docked, falling back to the modal overlay on narrow terminals.
